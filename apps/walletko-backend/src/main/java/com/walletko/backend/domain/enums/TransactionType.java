package com.walletko.backend.domain.enums;

public enum TransactionType {
    income,
    expense,
    transfer,
    canceled_income,
    income_cancellation,
    canceled_expense,
    expense_cancellation
}
